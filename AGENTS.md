# TY_BI 项目级多窗口协作、风险分级与模型调度规范

本规范为 **TY_BI 项目长期通用规则**，适用于当前及后续所有 Phase、开发节点、PR、新功能、缺陷修复、技术重构、测试验收、发布准备和生产问题处理，不限于 Phase9。

后续各阶段的专项任务书可以在本规范基础上增加更严格的要求，但不得无理由降低本规范规定的架构、安全、测试和验收门禁。

## 一、基本原则

项目采用风险分级调用机制，不再默认要求每个任务同时启动项目经理、架构、开发和测试四个窗口。

执行原则：

- 小型修改由开发窗口实施并自测。
- 普通功能由开发窗口实施，测试窗口定向验收。
- 涉及公共契约、核心运行时、数据库、安全、兼容性及 P0/P1 风险时，强制架构审查和正式测试验收。
- 全量测试、E2E 和生产构建仅在 PR 合并、节点关闭、Phase 验收、生产发布或核心公共模块变化时执行。
- 架构和测试窗口应基于任务书、commit diff 和验收清单工作，禁止无必要地重复扫描全仓库、重复执行相同测试或输出重复总结。
- 原则上只有开发主窗口修改业务代码；其他窗口以规划、审查、验证和证据输出为主。

## 二、风险等级

### 低风险

包括：

- 文案、样式和展示调整
- 明确的小型 Bug
- 局部重命名
- 文档和配置修正
- 不影响公共接口的简单测试补充

流程：

```text
项目经理登记
→ 开发窗口实施
→ 开发窗口定向自测
→ 项目经理确认
```

默认不启动架构窗口和独立测试窗口。

### 中风险

包括：

- 普通业务功能
- 局部组件能力
- 非核心 API 调整
- 影响范围明确的局部重构
- 不改变公共契约的数据处理调整

流程：

```text
项目经理明确任务
→ 开发窗口实施并定向测试
→ 测试窗口定向验收
→ 项目经理关闭
```

架构窗口按需介入。

### 高风险

包括：

- Schema、数据模型、公共 API 或持久化契约变化
- 设计态与运行态边界变化
- 核心运行时、事件机制、刷新、缓存或状态管理变化
- 数据库、SSL、权限、安全和存储机制变化
- 版本兼容、数据迁移和跨核心模块重构
- 可能影响多个页面、组件或业务模块的公共改动

流程：

```text
架构窗口冻结方案
→ 开发窗口实施
→ 架构窗口审查关键差异
→ 测试窗口正式验收
→ 项目经理决定放行
```

### P0/P1 或生产风险

包括：

- 数据结果可能错误
- 数据可能丢失或被覆盖
- 安全、权限、SQL、SSL 或凭据风险
- 无限循环、并发冲突、事务或回滚失效
- 生产故障和紧急修复
- 正式发布、阶段关闭和生产验收

流程：

```text
项目经理停线并定义门禁
→ 架构窗口确认方案
→ 开发窗口修复
→ 测试窗口提供完整证据
→ 架构窗口复核关键边界
→ 项目经理决定 accepted 或继续阻断
```

没有可追踪证据不得放行。

## 三、模型调度规则

项目经理每次派发任务时，应注明：

1. 任务风险等级
2. 需要启动的窗口
3. 各窗口建议使用的模型等级
4. 推理强度
5. 是否允许调用子代理
6. 测试范围
7. 是否需要架构审查
8. 验收证据由哪个窗口提供

模型选择原则：

- 低成本模型：文档、机械修改、日志整理、明确的小任务
- 常规模型：日常开发、普通审查、定向测试
- 高能力模型：复杂架构、跨模块重构、疑难问题、安全问题及 P0/P1
- 模型具体名称以客户端当前可选列表为准
- 小任务不得默认使用最高能力模型和最高推理强度
- 不得默认让四个窗口同时持续运行高消耗模型

## 四、测试规则

日常开发优先执行受影响范围的定向测试。

全量测试、E2E 和生产构建主要在以下场景执行：

- PR 准备合并
- 任一开发节点或里程碑准备关闭
- Phase 综合验收
- 核心公共模块发生变化
- Schema、运行时、数据库、安全或兼容机制变化
- P0/P1 修复完成
- 正式发布前

禁止每完成一个小步骤，就让多个窗口重复执行全量测试。

## 五、窗口交接标准

架构或测试窗口启动前，应提供：

```text
任务编号
风险等级
基线 commit
目标 commit
修改文件列表
git diff 范围
验收标准
开发窗口已执行的测试
已知问题和风险
需要重点审查的内容
```

架构窗口重点确认：

- 公共契约是否被破坏
- 模块和状态边界是否合理
- 兼容性是否保持
- 是否产生新的安全和架构风险

测试窗口重点确认：

- 验收条件是否满足
- 正常和异常路径是否正确
- 必要回归是否通过
- 测试证据是否完整

## 六、例外情况

以下情况可以采用特殊流程，但必须由项目经理明确说明原因：

- 紧急生产 P0 热修复
- 只用于验证可行性的技术试验
- 外部数据库、证书或第三方环境暂不可用
- 不修改代码的纯调研任务

特殊流程不能直接等同于正式验收。原型通过、代码已写、代码已提交和正式 accepted 必须继续区分。

## 七、禁止事项

禁止：

- 每个小任务默认启动全部窗口
- 多个窗口同时修改同一批业务代码
- 每个窗口重新扫描整个仓库
- 重复输出相同的长篇总结
- 多个窗口重复执行相同测试
- 没有契约变化却强制架构审查
- 没有风险依据就使用最高模型
- 用额度投入替代任务拆分和边界管理

## 八、执行节点人工介入门禁

- 不设置按执行时长强制暂停的时间门禁；执行窗口应持续提供可审查的阶段性进展和证据。
- 仅当同一根因连续失败达到 3 次、已尝试合理替代路径且仍无法解决时，才暂停并请求项目所有者人工介入。
- 客户端启动、默认模型不可用等发生在项目执行前的环境失败，不计入项目同一根因失败次数。
- 人工介入门禁的调整不降低架构、安全、测试和验收标准。

## 九、Phase 启动预演与规则分层

项目管理不得仅根据当前已暴露问题被动补充规则。项目经理应在每个 Phase 启动前，主动执行一次范围、依赖、风险、资源和流程预演，识别可能的架构冲突、测试缺口、并行修改风险、额度浪费和环境依赖。

项目级稳定规则应统一维护在本文件中，不得在每个 Phase 重复设计；阶段任务书只补充本阶段特有规则。发现新问题后，应先判断其属于个别问题还是项目级共性问题。属于共性问题时，应更新项目级治理规范，避免后续阶段重复发生。

每个 Phase 启动前至少检查：

- 产品范围
- 明确禁止项
- 前置依赖
- 代码基线
- 模块边界
- 兼容目标
- 环境条件
- P0/P1 风险
- 窗口调用方案
- 模型分配与推理强度
- 是否允许子代理
- 测试策略
- 验收证据及其责任窗口
- 回滚方式
- 并行修改与共享工作区风险
- 额度投入和重复工作的控制方式

Phase 启动预演必须形成可追踪记录，并明确 `go / conditional go / no-go` 结论。未完成预演或结论为 `no-go` 时，不得直接进入功能实施；`conditional go` 必须列出前置条件、责任窗口和关闭证据。

预演记录应优先引用已有项目级规则和架构决策，不重复复制稳定规则全文。阶段任务书只记录本阶段的范围、特有风险、节点拆分、专项门禁和回滚差异。

## 十、阶段完成通知与继续决策权

- 开发主窗口完成任一节点或阶段性开发后，必须向项目经理窗口提交完成报告和交接证据；若用户已明确授权当前 Phase 内自主推进，则项目经理可在节点门禁关闭后继续下一节点，否则必须停下等待决定。
- 完成报告至少包含：完成范围、未完成范围、修改文件、基线与目标状态、已执行测试、已知问题、风险以及建议的下一步。
- 项目经理负责汇总开发、架构和测试证据，明确区分“开发完成”“测试通过”“架构通过”和“正式 accepted”。
- 项目经理不得因当前 Phase 通过而自动进入下一 Phase；Phase 最终验收完成后必须停下汇报，由用户决定继续、调整、暂停或终止。
- 用户的显式阶段授权、暂停指令和范围调整具有最高决策权；未获得当前 Phase 内自主推进授权时，各窗口不得提前进入后续节点。
