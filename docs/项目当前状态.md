# 项目当前状态

当前业务版本为 V2.0，数据集、组件、图表、指标卡和数据计算底座已完成；V3 Phase7 模型、迁移、存储与参数体系升级已通过验收。

更新时间：2026-08-03

## 当前版本

V2.0。`package.json` 的工程版本仍为 `0.1.0`，后续应单独统一产品版本与包版本。

## 已完成

- 数据集 2.0：目录、检索、分页、编辑、复制、状态、字段语义和查询参数定义。
- 组件体系：公共配置、图表、指标卡、表格专属配置及医疗业务模板。
- 图表能力：多维度、多指标、系列拆分、组合图、双轴、气泡图、标签和预警线。
- 指标卡：单位、格式、同比、环比、目标值和进度。
- 数据计算：分组、排序、SUM、AVG、MIN、MAX、COUNT、COUNT DISTINCT 和 NONE。
- 画布交互：八方向缩放、画布与标题自定义、本地保存、导入导出和预览。
- 数据链路：PostgreSQL/Greenplum 只读查询、服务端数据集执行和真实组件绑定。
- V3 Phase7：V3 JSON、Schema、V1/V2 迁移、备份回退、参数中心及设计器兼容接入。

## 当前进行

Phase8 实施中：当前进入 P8.0 范围与基线，随后依次实现参数运行值、筛选控件、数据集参数绑定、安全查询条件生成、定向刷新和短期查询缓存。

Phase8 以 [Phase8 实施方案](./02_V3架构/Phase8实施方案.md) 为当前执行依据。Phase7 交付范围与结果见 [Phase7 交付说明](./02_V3架构/Phase7交付说明.md) 和 [Phase7 最终验收记录](./04_测试验证/验收记录/Phase7最终验收记录.md)。

## 未完成

- 参数运行时、筛选控件、数据集参数绑定和安全查询条件生成。
- 页面管理、统一事件、联动、下钻、导航和弹窗。
- 编辑、预览与发布运行时分离，以及不可变发布快照。
- 权限、查询缓存、AI 语义层和算法分析能力。

## 当前技术栈

前端：

- Vue 3.5、TypeScript 5.9、Vite 7。
- Vue Router、Tabler UI、Tabler Icons。
- ECharts 6。

后端：

- Node.js ESM 原生 HTTP 服务。
- `pg` 8.x 连接 PostgreSQL/Greenplum。
- 本机 JSON 保存数据源和数据集元数据。
- AES-256-GCM 加密数据库密码。

数据库：

- PostgreSQL/Greenplum。
- 固定验证库连接信息由本机私有配置提供；公开仓库只保留脱敏示例。
- Phase7 不新增数据库表。

## 验证状态

- 2026-08-03 执行 `npm run build` 通过。
- 62 项自动化测试全部通过，依赖安全审计为 0 个漏洞。
- P7.5 参数中心页面完成搜索、新建、编辑、复制、删除、系统模板、参数校验和刷新持久化验收。
- 参数操作期间记录到 0 个网络请求，确认未触发数据集查询或组件刷新。
- P7.6 已完成 V2 示例导入、V3 保存与刷新、组件新增持久化及参数保留回归。
- 设计器高级面板确认输出 `version: 3` 根模型，页面控制台无应用错误。
- V3 交付示例完成界面导入、保存、刷新和实际 `.v3.json` 文件下载验收。
- 500 个组件迁移和 200 个参数注册、搜索、编辑性能测试通过。
- 构建存在大体积 Chunk 告警，当前不阻塞 Phase7。

## 仓库状态

GitHub 公开仓库为 `sunjinhui0902/medical-bi-designer`，默认分支 `main` 已启用 PR、CI、禁止强推和禁止删除保护。Phase7 使用独立功能分支开发。

真实数据库配置、凭据、本地状态和内部验证资料继续由 `.gitignore` 排除。公开仓库采用“保留全部权利”的源码可见模式。
