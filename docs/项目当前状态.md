# 项目当前状态

当前业务底座为 V2.0，V3 Phase7—Phase10 已完成阶段验收并进入远端 `main`。项目已经具备应用模型、可信参数与查询运行时、多页面事件以及完整预览交互能力；Phase11 发布与治理尚未启动。

更新时间：2026-08-26

## 当前版本

V2.0。`package.json` 的工程版本仍为 `0.1.0`，后续应单独统一产品版本与包版本。

## 已完成

- 数据集 2.0：目录、检索、分页、编辑、复制、状态、字段语义和查询参数定义。
- 组件体系：公共配置、图表、指标卡、表格专属配置及医疗业务模板。
- 图表能力：多维度、多指标、系列拆分、组合图、双轴、气泡图、标签和预警线。
- 指标卡：单位、格式、同比、环比、目标值和进度。
- 数据计算：分组、排序、SUM、AVG、MIN、MAX、COUNT、COUNT DISTINCT 和 NONE。
- 画布交互：八方向缩放、画布与标题自定义、本地保存、导入导出和预览。
- 数据链路：PostgreSQL/Greenplum 只读查询、服务端数据集执行和真实组件绑定。
- V3 Phase7：V3 JSON、Schema、V1/V2 迁移、备份回退、参数中心及设计器兼容接入。
- V3 Phase8：参数运行值、数据集参数绑定、筛选控件、安全服务端聚合、定向刷新、请求合并和短期缓存。
- V3 Phase9：多页面管理、受控事件编辑、事件运行时、SetParameter、显式 Refresh 和仅预览执行的设计器接线。
- V3 Phase10：页面 push/replace/back、跨页参数、组件联动与清除、声明式 DrillPath、下钻栈、面包屑、Dialog Runtime 及安全新窗口/外链。
- 页面栈、弹窗栈、下钻栈、联动状态和弹窗几何均保持会话态，不写回 V3 JSON。
- 通用产品增强：多看板本机工作区、页签内容容器、自适应预览、20 × 20 组件最小尺寸、动态选项、表格分页与条件格式，以及地图和受控内容组件。

## 当前阶段

Phase10 P10.0—P10.7 已正式 `ACCEPTED`，架构与测试最终风险为 `P0=0 / P1=0 / P2=0`。交付提交 `c24865f` 经 PR #13 的 GitHub CI `verify` 通过后，于 2026-08-13 squash 合并到 `main`，合并提交为 `e455928`。

当前停止在 Phase11 之前。Phase10 的执行依据、验收矩阵和最终裁决分别见 [Phase10 实施方案](./02_V3架构/Phase10实施方案.md)、[Phase10 验收记录](./02_V3架构/Phase10验收记录.md) 与 [Phase10 最终验收](./02_V3架构/Phase10最终验收.md)。

公开 PR #14 正在交付 Phase10 之后的通用可用性增强，包括多看板本机工作区、直接新建画布、图表与表格配置、动态筛选、受控内容组件、自适应预览和 Tab 内容容器。公开提交不包含具体业务看板、真实数据库连接信息、内部 SQL 或私有数据，也不表示 Phase11 已启动。

## 待开发

- Phase11：不可变发布快照、发布态执行、版本、回滚与发布治理。
- 身份认证、RBAC、资源授权、多租户和审计。
- 生产部署、监控告警、配额、成本控制和灾难恢复。
- AI 语义层、自然语言分析、算法分析和智能看板生成。

## 当前技术栈

前端：

- Vue 3.5、TypeScript 5.9、Vite 7。
- Vue Router、Tabler UI、Tabler Icons。
- ECharts 6。

后端：

- Node.js ESM 原生 HTTP 服务。
- `pg` 8.x 连接 PostgreSQL/Greenplum。
- 本机 JSON 保存数据源和数据集元数据。
- AES-256-GCM 加密数据库密码。

数据库：

- PostgreSQL/Greenplum。
- 固定验证库连接信息由本机私有配置提供；公开仓库只保留脱敏示例。
- Phase7 不新增数据库表。

## 验证状态

- 2026-08-26 公开功能增强门禁通过：Node 308/308、无私有数据库凭据 Chromium 24/24、生产构建 7034 modules、官方 npm registry 高危依赖审计 0 漏洞。公开分支已移除业务看板、真实数据库连接实现和依赖私有数据库的测试。
- 2026-08-21 Tab 容器拖拽高风险节点门禁通过：Node 286/286、Chromium 24/24、生产构建 7012 modules、官方 npm registry 生产依赖审计 0 漏洞、`git diff --check` 通过。真实 Chromium 覆盖取消/切页完整回滚、上下左右四种标题方向边界及默认组合图可直接拖入。
- 2026-08-21 Tab 容器独立终审通过：架构 `ACCEPTED（P0=0 / P1=0 / P2=0）`；产品 `ACCEPTED（P0=0 / P1=0 / P2=5）`，MVP 可供用户复检。P2 为一致性和高级能力增强，不阻塞当前推送前审计。
- 2026-08-17 本轮可用性加固门禁通过：Node 280/280、Chromium 21/21、生产构建 7010 modules、官方 npm registry 生产依赖审计 0 漏洞。
- 本轮未新增生产依赖、未使用 PostgreSQL 凭据；生产构建仍有既有的大体积 Chunk 告警，不阻塞当前功能验收。
- Phase10 最终 `npm run verify:full` 通过：Node 268/268、Chromium 19/19、生产构建 7006 modules、官方依赖审计各级别均为 0。
- Phase10 架构与测试最终双签 `ACCEPTED`，P0/P1/P2 均为 0；未使用 PostgreSQL 凭据，未新增生产依赖。
- GitHub PR #13 CI `verify` 成功，远端 `main` 已更新到 `e455928`。
- 2026-08-12 执行 `npm run verify:node -- P9.7`：193/193 通过，生产构建通过。
- P9.7 Chromium 定向 E2E 2/2、P9.2 页面切换回归 1/1 通过，测试服务均自行退出并关闭 5174/5175。
- Phase9 最终 `npm run verify:full` 通过：Node 193/193、Chromium 13/13、生产构建、官方依赖审计 0 漏洞，5174/5175 均关闭。
- P9-FG1 全量门禁退出专项及 Phase9 整阶段均已由架构与测试双重 accepted，P0/P1/P2 均为 0。

- 2026-08-03 执行 `npm run verify:full` 通过。
- Node 单元与契约测试 95/95、Chromium 关键链路 5/5、生产构建通过，依赖审计为 0 个漏洞。
- Phase8 参数运行时支持默认值与系统值初始化、批量原子提交、相同值去重、清空可选参数、事务 ID 和可定位校验错误。
- 结构化查询计划在服务端完成参数条件、分组、聚合、排序和限制；600 行合成数据 SUM、COUNT 与 TOP 对账通过。
- 参数控件导入、提交刷新、保存和重载通过浏览器验收，运行值未写回 V3 JSON。
- P7.5 参数中心页面完成搜索、新建、编辑、复制、删除、系统模板、参数校验和刷新持久化验收。
- 参数操作期间记录到 0 个网络请求，确认未触发数据集查询或组件刷新。
- P7.6 已完成 V2 示例导入、V3 保存与刷新、组件新增持久化及参数保留回归。
- 设计器高级面板确认输出 `version: 3` 根模型，页面控制台无应用错误。
- V3 交付示例完成界面导入、保存、刷新和实际 `.v3.json` 文件下载验收。
- 500 个组件迁移和 200 个参数注册、搜索、编辑性能测试通过。
- 构建存在大体积 Chunk 告警，当前不阻塞 Phase7。

## 仓库状态

GitHub 公开仓库为 `sunjinhui0902/medical-bi-designer`。远端 `main` 已包含 Phase7—Phase10，当前提交为 `e455928`；Phase10 通过 PR #13 和 CI 后合并。当前本地功能分支仍指向合并前交付提交 `c24865f`，其文件树与 squash 合并结果一致。

真实业务看板、数据库连接配置、凭据、本地状态和内部验证资料不进入公开分支。公开仓库采用 GNU Affero General Public License v3.0（AGPL-3.0）。
