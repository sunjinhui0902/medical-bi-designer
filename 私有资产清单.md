# 私有资产清单

本清单定义 Medical BI Designer 本地开发资产与公开仓库的边界。清单中的私有资产不得提交、打包到 Release，或复制进公开 Issue、日志和截图。

## 必须排除

| 路径或资产 | 内容 | 当前保护 | Release 前动作 |
| --- | --- | --- | --- |
| `server/.data/` | 数据源、数据集本机状态和加密密钥 | 已被 `.gitignore` 排除 | 保持排除，禁止制作公开样例副本 |
| `server/.data/secret.key` | AES-256-GCM 本机密钥 | 已被父目录排除 | 禁止提交、复制或备份到 Release |
| `.step5-backup/` | 历史源码和开发备份 | 已被 `.gitignore` 排除 | 保持本地，发布包不包含 |
| `.vite/` | PID、日志、缓存和开发输出 | 整个目录已被 `.gitignore` 排除 | 保持排除 |
| `dist/` | 本机构建产物 | 已忽略 | Release 如需产物必须由 CI 重建 |
| `node_modules/` | 本地依赖 | 已忽略 | 不打包 |
| `*.log` | API 与开发日志 | 已忽略 | 公开问题前人工脱敏 |
| `.env*` | 环境变量和密钥 | 当前 `.env` 已忽略 | 扩展为忽略所有真实环境文件，仅保留 `.env.example` |
| Windows Credential | 数据库密码或凭据目标 | 不在仓库内 | 文档只写配置方法，不写真实值 |

## 需要持续排除的私有资产

以下文件目前没有密码，但包含私有基础设施标识，正式公开前必须脱敏或排除：

| 文件 | 敏感内容 |
| --- | --- |
| `server/validation-database.json` | 公网/专网地址、端口、数据库、用户名和数据源 ID |
| `docs/04_测试验证/固定验证数据库.md` | 固定验证库地址、用户名和数据源 ID |
| `server/.data/datasources.json` | 真实连接元数据和加密密码 |
| `server/.data/datasets.json` | 内部 SQL、字段、数据集命名和业务口径 |

前两项已通过精确 `.gitignore` 规则排除，并提供了脱敏的 `.example` 替代文件。发布前仍需用 `git status` 和首个提交清单确认它们没有进入版本库。不要仅依赖“没有密码”判断为可公开。

## 需要人工审查

- `docs/04_测试验证/截图/`：检查患者、医生、科室、机构、内部域名、IP 和账号。
- `docs/01_V2版本/示例/`：检查真实指标、内部表名、数据集 ID 和业务编码。
- `docs/05_AI规划/`：检查未公开产品路线、合作方或模型供应商约束。
- 医疗业务组件与 Demo 数据：确认全部为合成或授权数据。

## 可以公开的替代物

- 使用 `validation-database.example.json`，地址采用 `127.0.0.1` 或 `db.example.invalid`。
- 使用 `.env.example`，只保留变量名和占位值。
- 使用合成数据和虚构机构名称。
- 使用不包含本机路径、IP、用户名和数据源 ID 的截图。

## 处理原则

1. 私有文件使用忽略规则和本机生成机制保护。
2. 公开示例与真实配置使用不同文件名，不能靠手工覆盖同一文件。
3. 发现敏感信息进入 Git 后，删除工作区文件并不足够，必须清理历史并轮换凭据。
4. Release 产物应从干净检出环境构建，不从当前工作目录直接压缩。
